---
apiVersion: v1
kind: Service
metadata:
  name: main
  namespace: canary-ingress-nginx
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 8000
  selector:
    app.kubernetes.io/name: django
    app.kubernetes.io/instance: main
